[pytest]
testpaths = tests
addopts = -m "not live_api"
markers =
    live_api: tests that call external model APIs or depend on real credentials
    credit_canary: minimal paid live completion checks for provider credits and quota
    regression: deterministic game-logic regression tests that must run locally/in-container
